# Logistika Express Backend

Class-based Express API for the Logistika panels.

Run:

```bash
npm run backend:dev
```

Default URL:

```txt
http://127.0.0.1:4001/api/health
```

The backend uses the existing Prisma schema and `DATABASE_URL`, and keeps the same auth cookie name as the Next app: `azlog_token`.
