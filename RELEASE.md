# Production release provenance

This source snapshot was recovered from the running `logistika-production-app-1`
container on 2026-08-04. It corresponds to image digest
`sha256:2f7eadfbbc43104809a1a4f6e6762d46f1861a492b787e91e69cd84f34c99c9f`.

Runtime secrets, dependency directories, build output, uploaded files, and
persistent application data are deliberately excluded. Portainer supplies the
required environment variables at deployment time.

The deployment entrypoint is `compose.portainer-live.yml`.
